@model IEnumerable<mC_Connect_table.Models.RestaurantTables>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TableNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TableNumber)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TableNumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TableNumber">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TableNumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
